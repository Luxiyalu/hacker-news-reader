import { storage } from '../';

describe('StorageService', () => {
  it('init with localStorageData if already exists', () => {});

  it('init with clean data if localStorage data does not exist', () => {});

  it('#setLatestStories stores the latest 500 story IDs in localStorage', () => {});

  it('#getLatestStories returns an array of the latest 500 stories IDs', () => {});

  it('#setStory stores and only stores the necessary part of story data in localStorage', () => {});

  it('#getStoryById gets a story object by its ID', () => {});
});
